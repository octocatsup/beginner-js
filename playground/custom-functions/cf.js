// Function definition
function calculateBill() {
  // This is the function body
  let total = 100 * 1.0825;
  return total;
}

// Function call or "Run"
const billTotal = calculateBill();
console.log(`Your total is $${billTotal}`);
